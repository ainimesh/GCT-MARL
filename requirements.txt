# GCT-MARL Python dependencies (pip).
#
# Tested with Python 3.12, PyTorch 2.8 (CUDA 12.x). Earlier versions
# (torch >= 1.12) should also work.
#
# Install PyTorch matching your CUDA toolkit from https://pytorch.org first,
# then `pip install -r requirements.txt`.

numpy
torch>=1.12
matplotlib>=3.5
pandas>=1.3

# --- StarCraft Multi-Agent Challenge (SMAC) ----------------------------------
# SMAC is NOT the PyPI package literally named "smac" (that is an unrelated
# hyper-parameter optimizer). Install the oxwhirl SMAC, which pulls in pysc2:
#
#   pip install "git+https://github.com/oxwhirl/smac.git"
#
# You also need the StarCraft II game binary + the SMAC maps — see the README.

